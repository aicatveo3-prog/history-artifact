# assets/ 폴더의 모든 파일은 Git LFS로 저장합니다.
# (300MB 같은 큰 파일이 GitHub의 100MB push 제한에 걸리지 않게 하기 위함)
assets/** filter=lfs diff=lfs merge=lfs -text

# 아래 도우미 파일들은 일반 텍스트로 유지 (LFS 제외)
assets/.gitkeep text -filter -diff -merge
assets/README.md text -filter -diff -merge
